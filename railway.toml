[build]
builder = "DOCKERFILE"
dockerfilePath = "bot/Dockerfile"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[environments.production]
# Railway автоматично передає PORT змінну
